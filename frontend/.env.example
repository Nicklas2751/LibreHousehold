# Backend API URL for development (mockapi container)
VITE_API_URL=http://localhost

# OIDC authority for oidc-client-ts (dev: backend origin; prod: same as frontend origin)
VITE_AUTHORITY=http://localhost:8080
